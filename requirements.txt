# Core ML / Data Science
numpy>=1.24.0
pandas>=3.0.3
scikit-learn>=1.3.0
scipy>=1.11.0
networkx>=3.1

# Gradient boosting (§6.2 — primary predictive models)
xgboost>=2.0.0
lightgbm>=4.0.0

# Explainability (§9.5 — SHAP)
shap>=0.43.0

# Anthropic Claude API (§14.3 — RAG + conversational AI)
anthropic>=0.36.0

# REST API (Layer 1 — Experience)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Vector store for RAG (Layer 3 — AI Services)
chromadb>=0.4.22

# MLOps / experiment tracking (§9.2)
mlflow>=2.11.0

# Time-series (DemandForecaster fallback)
statsmodels>=0.14.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: streaming (production Kafka integration)
# kafka-python>=2.0.0

# Optional: graph database (production Neo4j integration)
# neo4j>=5.0.0
